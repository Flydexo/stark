type t = Z.t;;
let order = Z.of_string "3145729";;
let zero = Z.zero;;
let one = Z.one;;
let generator = Z.of_int 5;;
let (+) x y = Z.(mod) (Z.add x y) order;;
let neg x = Z.(-) order x;;
let (-) x y = (+) x (neg y);;
let ( * ) x y = Z.(mod) (Z.mul x y) order;;
let (/) x y = Z.(mod) (Z.divexact x y) order;;
let ( ** ) x n = Z.powm x n order;;
let inv x = Z.invert x order;;
type t = Z.t;;
let order = Z.of_string "3221225472";;
let modulus = Z.of_string "3221225473";;
let zero = Z.zero;;
let one = Z.one;;
let generator = Z.of_int 5;;
let (+) x y = Z.(mod) (Z.add x y) modulus;;
let neg x = Z.(-) modulus x;;
let (-) x y = (+) x (neg y);;
let ( * ) x y = Z.(mod) (Z.mul x y) modulus;;
let (/) x y = Z.(mod) (Z.divexact x y) modulus;;
let ( ** ) x n = Z.powm x n modulus;;
let inv x = Z.invert x modulus;;